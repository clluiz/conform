Algoritmo para rotas:

- Estrutura de pastas do projeto:

  /src
    /core
      /scripts
      /css
    /components
    /assets
    app.js
    index.html
    index.js

- Comparar cada fragmento da url passada com os fragmentos correspondentes do padrão;
- Regex quebrar path /\/[(^:)a-zA-Z0-9-]{1,}/g
- Usar regex nos patterns das rotas e comparar cada fragmento usando match

